

// Each cpu will have one allocated and each will have a forced idle state
// The idle state is controlled with a file
// A worker will be assigned to update the c-states from files 
struct typedef {

} StudyIdleDevice;